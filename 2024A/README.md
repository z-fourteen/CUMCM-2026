# 2024 CUMCM A题 · 问题一：板凳龙沿阿基米德螺线盘入

2024 年全国大学生数学建模竞赛 A 题（问题一）完整求解。板凳龙共 223 节板凳（1 龙头 + 221 龙身 + 1 龙尾），以把手销接；本问题研究全部 **224 个把手中心** `P_0, …, P_223` 的运动学。

## 问题简述

- 龙头前把手 `P_0` 从阿基米德螺线第 16 圈起点 `A = (8.8, 0)` 出发，以 **恒线速 1 m/s** 沿螺线顺时针向内盘入。
- 螺距 `p = 0.55 m`，螺线参数 `a = p/(2π)`，`r = aθ`。
- 相邻把手中心间距为**刚性弦长**：龙头 `L_1 = 3.41 − 2×0.275 = 2.86 m`，龙身/龙尾 `L = 2.20 − 2×0.275 = 1.65 m`。
- 需给出 `t = 0, 1, …, 300 s` 共 301 个时刻全部把手的 x、y 坐标与速度大小，保留 6 位小数。

## 模型要点

1. **龙头运动**：由弧长原函数 `F(θ) = ½[θ√(1+θ²) + asinh θ]`，解非线性方程
   `a[F(32π) − F(θ₀(t))] = t` 得到 `θ₀(t)`（brentq 求根）。
2. **龙身递推**：相邻把手满足**欧氏弦长**约束（而非弧长约束）
   `a²[θᵢ² + θᵢ₋₁² − 2θᵢθᵢ₋₁cos(θᵢ−θᵢ₋₁)] = Lᵢ²`，
   取 `θᵢ > θᵢ₋₁` 的**最近正向根**，逐节递推 `θ₀ → θ₁ → … → θ₂₂₃`。
3. **速度**：由刚性杆约束 `dᵢ·(Vᵢ − Vᵢ₋₁) = 0`（`Vᵢ = vᵢTᵢ`，`Tᵢ` 为盘入方向单位切向量）解析递推
   `vᵢ = vᵢ₋₁·(dᵢ·Tᵢ₋₁)/(dᵢ·Tᵢ)`，`v₀ = 1`；并用隐函数微分法与中心差分交叉验证。

## 文件说明

| 文件 | 说明 |
|---|---|
| `solve_q1.py` | 完整求解程序（建模 → 求解 → 验证 → 输出 Excel → 论文表格 → 绘图） |
| `result1.xlsx` | 结果表：Sheet「位置」（224 节点 × 301 时刻的 x/y）、Sheet「速度」（224 × 301），6 位小数 |
| `q1_positions.png` | t = 0/60/120/180/240/300 s 的板凳龙位形（红=龙头前把手，绿=龙尾后把手） |
| `q1_velocities.png` | 代表把手速度随时间变化曲线 |

## 运行方法

```bash
pip install numpy scipy pandas openpyxl matplotlib
python solve_q1.py
```

程序会自动覆写 `result1.xlsx`，并打印论文所需的代表表格与全部验证指标。

## 数值验证结果

| 检查项 | 结果 |
|---|---|
| 刚性距离最大残差 | 2.2e-13 m |
| 龙头弧长约束最大误差 | 2.0e-13 m |
| 节点顺序 `θ₀ < … < θ₂₂₃` | 恒成立 |
| 解析速度 vs 中心差分相对误差 | ≤ 5.6e-9 |
| 速度递推（方法一）vs 隐函数微分（方法二）偏差 | 6.2e-15 m/s |

## 关键结果（代表节点，6 位小数）

| 节点 | t=0 x | t=0 y | t=300 x | t=300 y |
|---|---|---|---|---|
| 龙头 `P_0` | 8.800000 | 0.000000 | 4.420274 | 2.320429 |
| 第 1 节龙身 `P_1` | 8.363824 | 2.826544 | 2.459489 | 4.402476 |
| 第 51 节 `P_51` | −9.518732 | 1.341137 | −6.301346 | 0.465829 |
| 第 101 节 `P_101` | 2.913983 | −9.918311 | −6.237722 | 3.936008 |
| 第 151 节 `P_151` | 10.861726 | 1.828753 | 7.040740 | 4.393013 |
| 第 201 节 `P_201` | 4.555102 | 10.725118 | −7.458662 | −5.263384 |
| 龙尾（后）`P_223` | −5.305444 | −10.676584 | 1.785033 | 9.301164 |

速度方面：龙头前把手恒为 1 m/s；其余把手略小于 1 m/s，且越靠后越小（t=0 龙尾后 ≈ 0.999311，t=300 龙尾后 ≈ 0.996478），这是刚性弦与螺线弧长差异随曲率增大而累积的物理结果。
